namespace Domain.Common;

/// <summary>
/// Represents an aggregate root in the domain-driven design pattern.
/// </summary>
/// <remarks>An aggregate root is the main entity that controls access to a group of related entities. It ensures
/// the integrity of the aggregate by enforcing business rules and invariants.</remarks>
public interface IAggregateRoot
{
}
